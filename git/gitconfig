[user]
    email = 31370233+vienai8d@users.noreply.github.com
    name = vienai8d
[color]
    ui = auto
[core]
    excludesfile = ~/.gitignore_global
    editor = vim
[alias]
    a = add
    aa = add .
    b = branch
    bd = !"git branch | grep -v '^*' | xargs git branch -D"
    c = checkout
    cb = checkout -B
    cdev = checkout -f origin/master -B develop
    cf = checkout -f
    cfo = checkout -f origin/master
    cfu = checkout -f up/master
    chr = cherry-pick
    coa = commit --amend
    com = commit -m
    d = diff
    f = fetch
    fo = fetch origin/master
    fp = fetch --prune
    fu = fetch up/master
    l = log --all --graph --pretty='format:%C(yellow)%h%C(cyan)%d %C(red)%ar     %C(reset)%s'
    me = merge
    p = push
    pf = push -f
    po = push origin
    pof = push origin -f
    re = rebase
    rei = rebase -i origin/master
    res = reset
    resh = reset --soft HEAD~
    s = status
